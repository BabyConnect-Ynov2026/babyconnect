# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=admin
DB_PASSWORD=changeme_in_production
DB_NAME=postgres
DB_SCHEMA=babyconnect

# Prisma (auto-built from above)
DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?schema=${DB_SCHEMA}"

# Server
SERVER_PORT=8080
GIN_MODE=release

# JWT (change this in production!)
JWT_SECRET=babyconnect-super-secret-key-2026
