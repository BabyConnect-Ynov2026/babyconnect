Pour installer **Docker** sur le Raspberry PI afin de conteneursier l'application suivre ces commandes :

```
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
docker version

```